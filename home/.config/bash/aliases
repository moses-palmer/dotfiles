alias diff='colordiff'
alias ll='ls -l'
alias ipython='python3 -m IPython'


carp() {
    cargo $@ --color=always 2>&1 | less -R
}

cd() {
  builtin cd "$@" && return
  [ "$OLDPWD" = "$PWD" ] || cdx "$@"
}
