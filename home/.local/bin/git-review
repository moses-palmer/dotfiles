#!/bin/sh
#
# Reviews a branch from the default remote.

set -e

# The start commit is the merge base for the default branch and the feature
# branch
START="$(git merge-base "$(git default-branch)" "$1")"

git dc --ignore-all-space "$START".."$1"
