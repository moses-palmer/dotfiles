#!/bin/sh

USAGE="Usage: $0 <FILE>

Lists other files part of the package managing <PATH>.
"
FILE="$1"


if [ -z "$FILE" ]; then
    echo "$USAGE"
else
    dpkg-query -L "$(dpkg -S "$FILE" | cut -d':' -f1)" \
        | awk '{ if (system("test -f " $1) == 0) { print $1; } }'
fi
